# python_fundamentals


## Functions

### 1. add_numbers
Adds two numbers and returns their sum.

```python
def add_numbers(num1, num2):



#### 2. is_even
checks if the number is even by using % which divides the number to check for remainers since even numbers wont give remainders.

### 3.reverse_string
reverses the string each character by slicing method text[::-1]



### 4. calculate_factorial
returns the factorial of a non-negative interger


### 5.count_vowels
counts numbr of vowels ignoring string case


### 6.apply_decorator
Applies a decorator that prints "Decorator Applied" before calling the original function.


### 7. sort_by_age
Applies a decorator that prints "Decorator Applied" before calling the original function.


### 8. merge_dicts
Merges two dictionaries and sums values for common keys.

### 9.Car Class
Defines a Car class with attributes make, model, and year, and a method display_info() to print car information.

